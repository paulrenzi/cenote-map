{
  "name": "cenote-conditions",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute cenote-conditions --file=schema.sql --remote",
    "db:init-local": "wrangler d1 execute cenote-conditions --file=schema.sql --local"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
